services:
  app:
    image: ghcr.io/herozuonan/delivery-system-mvp:${IMAGE_TAG:-dev}
    container_name: delivery-system-mvp
    ports:
      - "8080:8080"
    environment:
      - ADDR=:8080
    restart: unless-stopped
