# PM 最小输入包（Plan-to-Execute Gate）

> 目的：提供**可执行、可验收、可追溯**的最小输入，使工程活动可解除冻结并恢复推进。
> 硬规则：任一关键字段缺失 => **No-Go（继续冻结）**。

## 1) 迭代目标（必须）
- Iteration Goal：
- 目标Gate/交付物：
- 成功定义（可度量）：

## 2) 范围边界（必须）
- In Scope：
- Out of Scope（明确不做清单）：

## 3) 优先级与关键路径（必须）
- P0（阻断项）：
- P1（可延后）：
- 关键路径顺序：
- 阻断条件（Blocking Conditions）：

## 4) 验收口径与证据标准（必须）
- DoD（Definition of Done）总则：
- 必须产出证据清单（日志/报告/截图/签字等）：
- 证据存放与命名：遵循 `evidence/LOCK.md`
- 索引DONE标准：
  - 一跳定位（index → 证据文件）
  - 可复核（版本号/时间点一致）
  - 可追溯（决议 → 行动项 → 风险）

## 5) 风险与回滚要求（可选但推荐）
- 风险列表：
- 回滚要求：
